<UserControl x:Class="Skua.WPF.UserControls.ScriptSchedulerUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Skua.WPF.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Script Playlist Queue" FontSize="20" FontWeight="Bold" Margin="10"/>

        <TextBlock Grid.Row="1" Margin="10,0,10,5" Text="{Binding ScriptQueue.Count, StringFormat='Total Queued: {0}'}" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryHueMidBrush}" />

        <ListBox Grid.Row="2" ItemsSource="{Binding ScriptQueue}" Margin="10,0,10,10" 
                 Style="{StaticResource MaterialDesignListBox}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Margin="0,0,0,4" Padding="0,0,0,4">
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold" TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="1" Text="{Binding Status}" VerticalAlignment="Center" Margin="10,0" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                            <TextBlock Grid.Column="2" Text="{Binding Duration}" VerticalAlignment="Center" Margin="10,0"/>
                            <Button Grid.Column="3" 
                                    Style="{StaticResource MaterialDesignIconForegroundButton}" 
                                    Command="{Binding DataContext.RemoveScriptCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                                    CommandParameter="{Binding}" 
                                    Margin="10,0,0,0"
                                    Foreground="#e57373"
                                    ToolTip="Remove from queue">
                                <materialDesign:PackIcon Kind="Close" />
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Search Scripts" Command="{Binding OpenScriptRepoCommand}" Margin="0,0,10,0"/>
            <Button Content="Start Queue" Command="{Binding StartQueueCommand}" Margin="0,0,10,0" Style="{StaticResource MaterialDesignRaisedLightButton}"/>
            <Button Content="Stop Queue" Command="{Binding StopQueueCommand}" Style="{StaticResource MaterialDesignRaisedSecondaryButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
